<Window x:Class="WpfApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>

        <TextBlock Text="{Binding CheckBoxState, Mode=TwoWay}"  Margin="249,257,0,0" />
        <CheckBox Content="CheckBox" IsChecked="{Binding CheckBoxState, Mode=TwoWay}" HorizontalAlignment="Left" Margin="249,157,0,0" VerticalAlignment="Top"/>
    </Grid>
</Window>
